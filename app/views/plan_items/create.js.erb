<%- day_selector = "#day_#{@plan_item.day_id}" %>
$('<%= escape_javascript render(@plan_item) %>')
  .hide()
  .appendTo('<%= day_selector %> ul')
  .fadeIn();
$('<%= day_selector %> form').resetForm();
